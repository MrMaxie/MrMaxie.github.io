<article class="article-content">
    <slot />
</article>

<style lang="scss" is:global>
    article.article-content {
        .astro-code {
            padding: 1rem;
            margin: 0 1rem;
            margin-bottom: 1rem;
            border-radius: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            background-color: #24292e;
            background: radial-gradient(circle at right bottom, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.05) 30%, rgba(255, 255, 255, 0) 100%);
            position: relative;

            &:after {
                content: attr(data-language);
                display: block;
                position: absolute;
                color: rgba(0, 0, 0, 0.1);
                right: 0;
                bottom: 0;
                font-size: 3rem;
            }
        }

        h2, h3 {
            margin-bottom: 1rem;
        }

        p {
            padding: 0 2rem;
            margin: 0;
            margin-bottom: 1rem;
            text-align: justify;
            font-size: 1.1rem;
            color: #ddd;
            font-weight: 300;

            strong {
                font-weight: 600;
            }
        }

        ul, ol {
            padding: 0 2rem;
            margin: 0;
            margin-bottom: 1rem;
            list-style: none;
            font-weight: 300;

            li {
                margin-bottom: 0.5rem;
                padding-left: 1rem;
                position: relative;
                text-align: justify;

                &::before {
                    content: '•';
                    position: absolute;
                    left: 0;
                    color: #ddd;
                }
            }
        }

        code {
            padding: 0.2rem 0.5rem;
            border-radius: 0.5rem;
            background-color: rgba(255, 255, 255, 0.1);
            font-size: 0.9rem;
        }

        pre code {
            padding: 0;
            background-color: transparent;
            border-radius: 0;
        }
    }
</style>
