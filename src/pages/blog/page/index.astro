---
import ArticleListing from '~/components/blog/ArticleListing.astro';
import { getArticlePages } from '~/lib/articles';

const firstPage = (await getArticlePages())[0];

if (!firstPage) {
    throw new Error('The blog requires at least one article.');
}
---

<ArticleListing {...firstPage} />
